{
  "name": "invoiceiguana",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Turn an invoice or receipt into a shareable link — the whole document lives in the URL.",
  "license": "UNLICENSED",
  "author": "Chris Whelan <cdwhelan@gmail.com>",
  "scripts": {
    "dev": "wrangler pages dev site",
    "selftest": "node scripts/selftest.mjs",
    "icons": "node scripts/make-icons.mjs",
    "deploy": "wrangler pages deploy site"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
